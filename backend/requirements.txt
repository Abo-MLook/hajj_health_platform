# Hajj Health Platform — backend dependencies
# Install with: pip install -r requirements.txt

# Web framework
Django==5.2.15

# Configuration
python-dotenv==1.2.2

# Media storage (Cloudinary)
cloudinary==1.44.2
django-cloudinary-storage==0.3.0
django-storages==1.14.6

# Text extraction
pdfplumber==0.11.9          # PDF text
pytesseract==0.3.13         # image OCR (requires Tesseract installed)
pillow==12.2.0              # image handling

# AI structured extraction (Groq — https://groq.com)
openai==2.41.0             # main extraction via Groq's OpenAI-compatible endpoint
groq==1.4.0                # review agent (native Groq SDK)

# QR code
qrcode==8.2                 # generate
pyzbar==0.1.9               # decode (requires zbar shared library)
